
# Unix Shell Usage (generated by codex-win-unix)

## Shell selection
- PowerShell 7 (default): Windows paths, .NET objects
- bash (MSYS2): Unix pipelines (grep/sed/awk/find), shell scripts
- WSL: Full Linux environment

## MSYS2 bash tools
bash 5.3, grep, sed, awk, find, curl, make, patch, diff, tar, gzip, bzip2, xz, openssh, vim, nano, less, which

## Proxy
Auto-enabled via .bashrc. Proxy: {{PROXY_URL}}

## Encoding
UTF-8 enforced everywhere.
